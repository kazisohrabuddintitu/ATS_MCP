# ATS_MCP
### Create virtual environment and activate it
### Install requirements.txt file
### Run in terminal '''uvicorn api_server:app --host 0.0.0.0 --port 8000 --reload'''
### Run in another terminal '''ngrok http 8000'''
Use the link given by ngrok as public API.